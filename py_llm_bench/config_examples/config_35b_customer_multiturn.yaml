# AUTO-GENERATED by sgl_benchmark/gen_customer_repro_configs.py
# DO NOT EDIT BY HAND.  Re-run the generator to refresh.
#
# Phase-A/B multiturn mode reproducing customer crash signature
#   Customer log: Prefill batch, #new-seq: 1, #new-token: 232, #cached-token: 53852
# Variant: 35b — 35B-A3B hybrid (hidden=2048). Smaller proxy for customer's 397B.
# Turn 1 carries a ~55K-token user message.  Turns 2-4 are short follow-ups
# that hit the radix cache for the bulk of the context (turn-1 user + assistant
# response), so #new-token is tens while #cached-token is ~55K — exactly the
# pattern that triggered the HSA_STATUS_ERROR_EXCEPTION on the customer rig.

image: "atom-sglang:opt2"
model_path: "/.cache/huggingface/Qwen3.5-35B-A3B-FP8"
model_prefix: "qwen35_customer_multiturn"
host_model_mount_path: "/raid/models"

run_mode: multiturn
precision: fp8
port: 37664
enable_thinking: false

health_timeout: 480
watchdog_timeout: 3600

container_env:
  - "SGLANG_EXTERNAL_MODEL_PACKAGE=atom.plugin.sglang.models"
  - "HF_HOME=/.cache/huggingface/"
  - "SGLANG_DISABLE_CUDNN_CHECK=1"
  - "SGLANG_USE_AITER=1"
  - "SGLANG_ROCM_USE_AITER_LINEAR_SHUFFLE=1"
  - "SGLANG_ROCM_USE_ALTER_LINEAR_FP8HIPB=1"
  - "SGLANG_USE_AITER_NEW_CA=false"
  - "SGLANG_AITER_FP8_PREFILL_ATTN=0"
  - "SGLANG_HEALTH_CHECK_TIMEOUT=600"
  - "SGLANG_WARMUP_TIMEOUT=1800"
  - "HIP_VISIBLE_DEVICES=4,5,6,7"
  - "CUDA_VISIBLE_DEVICES=4,5,6,7"

docker_run_args:
  shm_size: "128g"
  cap_add: ["SYS_PTRACE"]
  security_opt: ["seccomp=unconfined"]
  user: "root"
  devices: ["/dev/kfd", "/dev/dri"]
  group_add: ["video"]
  ipc_mode: "host"
  pid_mode: "host"
  network_mode: "host"
  privileged: true
  ulimits:
    core:
      soft: 0
      hard: 0

extra_container_mounts:
  - "/raid/users/jialzhu/code_backup/qwen397b-coredump/repro_caches/aiter_configs:/tmp/aiter_configs"
  - "/raid/users/jialzhu/code_backup/qwen397b-coredump/repro_caches/atom:/root/.cache/atom"

server_args:
  - "--tensor-parallel-size 4"
  - "--attention-backend aiter"
  - "--reasoning-parser qwen3"
  - "--tool-call-parser qwen3_coder"
  - "--trust-remote-code"
  - "--chunked-prefill-size 2048"
  - "--mem-fraction-static 0.85"
  - "--max-running-requests 256"
  - "--enable-metrics"
  - "--collect-tokens-histogram"

multiturn_turns_file: "/raid/users/jialzhu/code_backup/qwen397b-coredump/sgl_benchmark/py_llm_bench/config_examples/_customer_repro_assets/turns_multiturn.json"
